{
  "name": "fe-sharing",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "14.15.4",
    "npm": "6.14.10"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node generate-data.js && node server.js",
    "serve": "json-server --watch db.json --port 4000",
    "serve:routes": "json-server --watch db.json --port 4000 --routes routes.json",
    "generate-data": "node generate-data.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "5.5.0",
    "json-server": "^0.17.2",
    "query-string": "7.0.0"
  }
}
